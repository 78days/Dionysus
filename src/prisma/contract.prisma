
model Event {
  id            Int      @id @default(autoincrement())
  name          String
  slug          String   @unique
  city          String
  location      String
  date          DateTime
  imageUrl      String
  organizerName String
  description   String
  createdAt     DateTime @default(now())
  updatedAt     DateTime
}